# 영상을 불러온다

# loop

    # 이벤트 발생한 좌표에서 rgb를 파일 아웃풋으로 저장한다
    # 인자: 프레임(넘파이어레이), 마우스 클릭 이벤트

    # 한 프레임씩 스페이스바로 넘긴다


# 함수
# 이벤트 발생한 좌표에서 rgb를 반환
# 인자: 프레임(넘파이어레이), 마우스 클릭 이벤트
# 반환: rgb
def get_color()

# 함수
# 파일 아웃풋으로 저장한다
# 인자: 기록할 값: rgb, 튜플
# 반환: csv